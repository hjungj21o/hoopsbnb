<% if logged_in? %>
    <script type="text/javascript">
        window.user = <%= render(
            "api/users/user.json.jbuilder",
            user: current_user
        ).html_safe %>
    </script>
<% end %>

<main id="root"></main>